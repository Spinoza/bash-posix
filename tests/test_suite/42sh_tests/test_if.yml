if_else_easy:
        type: "bash diff"
        command: "if true; then echo true; else echo false; fi;"
        expected:
                stdout:
                stderr:
                rvalue:

if_else_command:
        type: "bash diff"
        command: "if true; then echo true; else echo false; fi; ls;"

        expected:
                stdout:
                stderr:
                rvalue:

if_else_false_command:
        type: "bash diff"
        command: "if false; then echo true; else echo false; fi; ls;"
        expected:
                stdout:
                stderr:
                rvalue:

elif_true:
        type: "bash diff"
        command: "if false; then echo true; elif true; then pwd; else ls; fi;"
        expected:
                stdout:
                stderr:
                rvalue:

if_no_else:
        type: "bash diff"
        command: "if true; then ls; fi;"
        expected:
                stdout:
                stderr:
                rvalue:

if_false_no_else:
        type: "bash diff"
        command: "if false; then ls; fi;"
        expected:
                stdout:
                stderr:
                rvalue:

starting_elif:
        type: "bash diff"
        command: "elif true; then ls; fi;"
        expected:
                stdout:
                stderr:
                rvalue:
