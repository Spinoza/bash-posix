easy_echo:
        type: "bash diff"
        command: "echo test"
        expected:
                stdout:
                stderr:
                rvalue:

easy_echo_semicolon:
        type: "bash diff"
        command: "echo test;"
        expected:
                stdout:
                stderr:
                rvalue:

echo_multiple_args:
        type: "bash diff"
        command: "echo word1 word2 word3 word4"
        expected:
                stdout:
                stderr:
                rvalue:
